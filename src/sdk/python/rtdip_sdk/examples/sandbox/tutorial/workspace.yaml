# workspace.yaml

#load_from:
#  - python_file: assets.py
#  - python_module: resources

load_from:
  # Each entry here corresponds to a service in the docker-compose file that exposes user code.
  - grpc_server:
      host: dagster_test
      port: 4000
      location_name: "dagster_test"
