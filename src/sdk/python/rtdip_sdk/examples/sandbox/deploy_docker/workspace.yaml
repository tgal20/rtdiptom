load_from:
  # Each entry here corresponds to a service in the docker-compose file that exposes user code.
  - grpc_server:
      host: docker_tutorial2_user_code
      port: 4000
      location_name: "tutorial_user_code"
